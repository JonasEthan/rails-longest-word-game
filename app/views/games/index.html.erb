<h1 class="text-center">NEW GAME</h1>
<div class="container d-flex justify-content-center">
  <%@letters.each do |letter|%>
  <div class="d-inline-flex card p-4 m-2 bg-info fw-bold text-white text-center"><%= "#{letter.upcase}"%></div>
  <%end%>
</div>
